{
  "name": "nations-league-pool-e2e",
  "private": true,
  "type": "module",
  "description": "Browser tests: builds the frontend, boots the real backend in demo mode and clicks through the app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
